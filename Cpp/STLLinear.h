#pragma once
#include "common.h"

class Vectors {
/* 

Just a list of uncommon operations in vectors 

vector<int> s(iter_first, iter_last)
s.front(); s.back();
s.pop_back();
s.insert(it, n, x); // insert n xs.
s.insert(it, it_first, it_last);
s.erase(it);
s.reverse(n);
s.resize(n);
s.clear();
s.swap(vector2);
s.assign(first, last); // replace

*/
};
